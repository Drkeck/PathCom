import Express from "express";
const router = Express.Router();
// ^ router ^ //

router.get("/", (req,res) => {
    // ^ get all skills ^ //
    try {

    } catch(error) {

    } finally {

    }
})